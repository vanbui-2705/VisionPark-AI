VITE_API_BASE_URL=http://localhost:8000
VITE_API_TIMEOUT_MS=15000
# Chỉ bật khi dev không có backend: fallback fixtures cho users/detections/audit (404/501)
VITE_USE_MOCK_FIXTURES=false
# Public registration phải tắt mặc định (client không bao giờ tự đăng ký ADMIN)
VITE_PUBLIC_REGISTRATION_ENABLED=false
