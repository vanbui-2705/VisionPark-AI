# AGENTS — Bootstrap

Agent mới:
1. Đọc `frontend/docs/overview.md`
2. Đọc `frontend/docs/ai-context.md`
3. Đọc `frontend/docs/architecture.md`
4. Đọc `frontend/docs/decisions.md`
5. Không quét toàn repo trước khi đọc 4 file trên.
6. Sau thay đổi đáng kể: cập nhật `frontend/docs/*.md` liên quan (routes/permissions/API).
7. Không fake số liệu production, không hard-code token/role/URL.

Docs: nguồn thật `frontend/docs/*.md`, render qua import.meta.glob raw.
