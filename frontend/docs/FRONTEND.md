# Frontend

- Entry: `src/main.tsx` → `App.tsx` → `AppRoutes`.
- Layout: `AppShell` (sidebar groups, header breadcrumb/health/notifications/user dropdown).
- Modules: `auth`, `dashboard`, `station`, `detections`, `lanes`, `users`, `roles`, `permissions`, `alpr`, `audit`, `system`, `profile`, `settings`, `notifications`, `help`, `docs`, `uikit`, `errors`.
- UI: `src/components/ui/*` + `index.css` tokens.
