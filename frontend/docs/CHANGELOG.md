# Changelog

## 2026-09-13 — Docs Center + unsaved guard
- Lấp toàn bộ `frontend/docs/*.md` bằng nội dung thực.
- Thêm alias tên chuẩn `PROJECT_CONTEXT.md` → `overview.md` v.v.
- Wire `useUnsavedGuard` vào `LaneForm`, `CreateUserPage`, `UserEditPage`.

## 2026-09-12 — 28-screen Control Center
- Router đầy đủ 28 routes + aliases + `AdminGuard` + `* → /404`.
- AppShell groups + collapse + drawer + header.

## 2026-09-11 — Auth + lanes + users portal
- Login/register, dashboard, lanes CRUD, users, roles/permissions, mock API.
